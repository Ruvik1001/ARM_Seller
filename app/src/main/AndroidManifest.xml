<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ruvik1001.arm_seller">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ARM_Seller"
        tools:targetApi="31">
        <activity
            android:name=".AboutCheckActivity"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MyProfileActivity"
            android:exported="false"
            android:label="Профиль" />
        <activity
            android:name=".PostuplenieActivity"
            android:exported="false"
            android:label="Поступления" />
        <activity
            android:name=".SkladActivity"
            android:exported="false"
            android:label="Склад" />
        <activity
            android:name=".Main_menu"
            android:exported="false"
            android:label="Главное меню" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>